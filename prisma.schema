type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
  posts: [Post!]! @relation(name: "UserToPost")
}

type Post {
  id: ID! @id
  title: String!
  content: String!
  author: User! @relation(name: "UserToPost")
}